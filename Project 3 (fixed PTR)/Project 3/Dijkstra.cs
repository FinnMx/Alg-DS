using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace Project_3
{
    class Dijkstra
    {/*
        public static int[] dijkstra(int x, MyList<MyList<AdjacencyListND>> graph, int source)
        {
            int[] distance = new int[x];
            for(int i = 0; i < x; i++)
            {
                distance[i] = Int32.MaxValue;
            }
            distance[source] = 0;

            SortedSet<AdjacencyListND> ss = new SortedSet<AdjacencyListND>();
            ss.Add(new AdjacencyListND(source, 0));

            while(ss.Count > 0)
            {
                AdjacencyListND current = ss.First();
                ss.Remove(current);


                foreach (AdjacencyListND n in graph.GetVal(current.GetVertex()))
            }

            


        }*/
    }
        
}
